#include<stdio.h>
#include<string.h>
int main()
{
    char str1[10001],str2[10001];
    scanf("%s",str1);
     scanf("%s",str2);
    
    if(strlen(str1)!=strlen(str2))
     return 1;
     
     for(int i=0;str1[i]!='\0';i++)
     {
       /*unsigned char result= str1[i] ^ str2[i];
        printf("%u",result);*/
        
        int bit1 = str1[i] - '0';
        int bit2 = str2[i] - '0';
        printf("%d", bit1 ^ bit2); 
        
     }
    
}